---
version: "3.9"

services:
  postgres:
    image: postgres:14
    ports:
      - 5432:5432
    environment:
      TZ: "UTC"
      POSTGRES_DB: "dev"
      POSTGRES_USER: "admin"
      POSTGRES_PASSWORD: "password"
