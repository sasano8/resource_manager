from pathlib import Path

PARSER_FILE = Path(__file__).absolute().resolve().parent / ".lark_cache.bin"
TEMPLATE_GRAPH_HTML_FILE = (
    Path(__file__).absolute().resolve().parent / "template_graph.html"
)
